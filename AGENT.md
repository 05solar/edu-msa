# AGENT.md · AI 에이전트 작업 규칙 (전체)

이 문서는 저장소 전체에서 AI 에이전트(및 사람 기여자)가 작업할 때 지켜야 하는
규칙을 정의한다. 각 하위 폴더(`frontend/`, `backend/`)에도 동일한 형식의 `AGENT.md`가
있으며, 하위 규칙이 더 구체적이면 하위 규칙이 우선한다.

## 0. 절대 규칙

- **이모지/이모티콘 금지.** 어떤 코드·문서·UI 텍스트에도 이모지를 넣지 않는다.
  아이콘이 필요하면 인라인 SVG 아이콘 세트를 사용한다.
- **디자인 원본을 임의로 바꾸지 않는다.** 시각 디자인은 PoC 원본과 동일하게
  유지하되, 이모지 아이콘만 SVG로 대체한다.

## 1. 파일·폴더 분리 규칙

- 프론트엔드: 컴포넌트/페이지 단위로 폴더를 만들고, 한 페이지 폴더에는
  `*.tsx`와 `*.css`를 **같은 폴더 안에 별도 파일**로 둔다.
- 백엔드: 기능(도메인) 단위로 패키지/폴더를 나눈다.
- 하나의 파일에 여러 화면·기능을 몰아넣지 않는다.

## 2. 메타 문서 리뉴얼 규칙 (중요)

**하나의 명령(작업)을 수행할 때마다** 관련된 메타 문서를 갱신한다.

- 전체에 영향: `docs/planning/PROCESS.md`에 작업 이력 1줄 추가.
- 프론트 작업: `frontend/PROCESS.md`(이력), 필요 시 `frontend/DESIGN.md`,
  `frontend/TEST.md`, `frontend/README.md`를 갱신.
- 백엔드 작업: `backend/PROCESS.md` 및 관련 문서 갱신.
- `AGENT.md`는 작업 규칙이 바뀔 때만 갱신.

메타 문서 종류와 목적:

| 파일 | 목적 |
| --- | --- |
| `README.md` | 해당 범위의 개요·실행법 |
| `docs/planning/PROCESS.md` | 작업 프로세스와 진행 이력(변경 로그) |
| `AGENT.md` | 에이전트 작업 규칙 |
| `DESIGN.md` | 디자인 원칙(프론트) / 설계 원칙(백엔드) |
| `TEST.md` | 테스트 전략과 체크리스트 |

## 3. 작업 절차

1. 관련 메타 문서를 먼저 읽는다.
2. 파일·폴더 분리 규칙에 맞게 구현한다.
3. 타입체크/빌드/테스트로 검증한다.
4. 관련 메타 문서를 갱신한다(2번 규칙).

## 4. 커밋

- 사용자가 요청할 때만 커밋한다.
- 기본 브랜치에서 바로 작업하지 않는다(필요 시 브랜치 생성).
