# PROCESS.md · 인프라(deploy) 진행 이력

`deploy/`는 플랫폼과 배포 파이프라인의 인프라 산출물을 담는다.

```
deploy/
├── docker-compose.yml     # 로컬: postgres·auth-db·auth-service·traefik·backend (docker 실배포 모드)
├── .env.example           # 시크릿 주입 예시(EDU_JWT_SECRET·EDU_SEED_PASSWORD 등)
└── k8s/
    ├── namespaces.yaml            # 기본 네임스페이스
    ├── auth/                      # 인증 계층(auth-db·auth-service·edu-auth-jwt Secret)
    ├── platform/                  # 플랫폼(postgres/backend/frontend/ingress/rbac)
    ├── service-template.yaml      # 서비스 1개당 렌더링 템플릿(보안 컨텍스트 포함)
    ├── hardening/                 # 멀티테넌트 보안 하드닝(PodSecurity/Quota/NetPol/RuntimeClass/Kaniko)
    └── README.md                  # K8s 배포 가이드
```

## 프로세스

1. 매니페스트는 **최소 권한·기본 차단** 원칙으로 작성한다.
2. 로컬 kind로 적용·검증 후, 실서버(Calico/Cilium, gVisor, 레지스트리)로 승격한다.
3. 변경 시 본 이력과 관련 문서(README/SECURITY/AGENT)를 갱신한다.

## 진행 이력 (Change Log)

- 2026-09-10 — readiness 3차(최종 GO 게이트 리허설): 실서버·운영 수신처 부재 확정(BLOCKER) 후 kind 멀티노드에서 런북 전 단계 실측 — GHCR git-5ac5c0e 무중단 롤아웃(digest 일치)·smoke 10/10·on-demand 백업+MinIO 오브젝트 실확인·PITR 정밀(T1/T2 복원·T3 부재, 41s)·failover(강제 kill→9s 승격, RPO 0, 앱 에러창 ~23s)·워커 kill 회수(1회 완료·중복 0)·NetPol 7/7·Redis 장애 드릴(로그인 +10s 결함 → redis timeout 250ms/connect 500ms 수정 → 0.40s 재검증)·replica +2,801 흡수·HPA 2→9→2·로그인 용량 실측(replica당 ~16/s)에 따라 autoscale.yaml 에 auth HPA(2–6)+PDB 신설. kubeconform Valid. 판정 CONDITIONAL GO 유지(환경 2건) — PRODUCTION_READINESS.md §12.
- 2026-09-10 — readiness 2차(Condition Closure): ① Alertmanager 수신 경로 — monitoring/alertmanager-values.yaml 신설(edu-webhook 라우팅, 수신 URL 은 Secret edu-alert-receiver 를 alertmanagerSpec.secrets 로 마운트해 url_file 로 읽음 — Git 평문 없음), bootstrap 은 Secret 존재 시에만 적용(부재 시 경고). staging 에서 합성 규칙 firing→AM→테스트 webhook POST 수신까지 전 구간 실검증. ② NAT rate-limit — auth Ingress limit-rps 5→20 + burst-multiplier 5 + limit-connections 10→100, auth env EDU_RATELIMIT_IP_MAX 30→200(실패 기준). 전/후 실측: 10/s 단일 IP 46% 차단→100% 통과, burst 50 동시 100%, brute-force(계정 5회 잠금) 유지, Hikari 피크 2/0·5xx 0. kubeconform 4리소스 Valid. 종합: PRODUCTION_READINESS.md §11.
- 2026-09-10 — 관측성 배선 수리(production readiness review): ① prometheus-rules.yaml 라벨 `release: kps`→`monitoring`(스택 릴리스명 불일치로 규칙이 전혀 선택되지 않던 결함 — 6431dff ServiceMonitor 와 동일 계열) + Production 필수 alert 10종 신설(AuthDown/WorkerDown/Http5xxHigh/HikariPendingSustained/DbReplicationLagHigh/DbInstanceDown/DeployQueueBacklog/DeployRetriesHigh/RedisFallbackActive/PvcAlmostFull). ② bootstrap.sh kube-prometheus-stack 에 `alertmanager.enabled=true` 명시 + `podMonitorSelectorNilUsesHelmValues=false`(CNPG PodMonitor 무라벨로 DB 메트릭 미수집 결함), 규칙 apply 의 `2>/dev/null || true` 무음 실패 제거. 검증(staging): 규칙 13종 로드 health=ok, Prometheus→Alertmanager 연결 활성+합성 알림 수신, cnpg_* 수집 개시(replication lag 0s). 추가 드릴: CNPG failover(primary 삭제→183s 승격→224s 3/3), PITR 복구(MinIO 백업→시점 복구 성공·시점 정확성 검증), stale replica 재클론(auth-db-3 timeline 분기 복구). 종합 판정은 docs/operations/PRODUCTION_READINESS.md (CONDITIONAL GO).
- 2026-09-09 — 워커 분리(13단계): platform/backend-worker.yaml 신설 — backend 와 동일 이미지·EDU_DEPLOY_WORKER_ENABLED=true, edu-deployer SA, 메트릭 Service + KEDA ScaledObject(edu_deploy_queue_depth 기반, KEDA 미설치 시 해당 문서만 실패하고 replicas 1 동작). backend.yaml 은 워커 오프. ServiceMonitor 를 backend/backend-worker 겸용으로 확장. bootstrap: 렌더 목록·apply 순서 추가, backend 전용 sed(단일 DB·kind 레지스트리·gitea 호스트)를 backend-worker 에도 적용. 검증: kubeconform 6리소스 Valid, bash -n.
- 2026-09-09 — read 분산(12단계): backend.yaml 에 DB_RO_URL(edu-db-pooler-ro — 7단계 ro 풀러 실사용) 추가, bootstrap 코어(단일 postgres)는 값을 비워 라우팅 비활성. frontend nginx 캐시·gzip 정책은 frontend/PROCESS.md, 라우팅 구조·실측은 backend/PROCESS.md 와 loadtest/RESULTS-20260909-local.md 참조.
- 2026-09-09 — 부하 실측·튜닝(11단계): 로컬 28코어 전용 환경(JAR×2 + pg×2 + redis, lt_ 시드 5만 계정/2천 프로그램)에서 rps100~2000 + 로그인 burst 100~450/s 실측. 병목 특정 — ① auth 로그인 @Transactional 내 bcrypt(~50ms)의 커넥션 점유로 풀 20 이 400/s 에서 고갈(active 20/20·pending 179·p95 12s), ② bcrypt CPU(코어당 ~21 로그인/s). 조회는 2,000 RPS 에서도 여유(캐시 적중 91%·Hikari active≤3·p95 5ms). 1변수씩 실험: auth 풀 40 채택(p95 12s→62ms·drop 0), backend 풀 10 채택(동일 성능·총량 여유), Tomcat 64 되돌림(무효과). 반영: auth/backend K8s env DB_POOL_MAX_SIZE 40/10, auth 풀러 rw pool_size 40 정렬, CNPG max_connections 200(서버측 총량 산술). replica/HPA/Quota 는 미측정이라 무변경 — auth HPA 신설·로그인 트랜잭션 분리를 차기 권장으로 기록(loadtest/RESULTS-20260909-local.md). 하네스 수정: k6 noCookiesReset(refresh 회전 체인 유지)·AUTH_URL 분리·capture-local.ps1 신설. 검증: kubeconform 12리소스 Valid.
- 2026-09-09 — 부하 테스트 체계(10단계): loadtest/ 신설 — k6 시나리오(mixed: 카탈로그 목록35%·검색20%·상세20%·counts10%·알림10%·refresh4%·로그인1% 비중, RPS 기반 constant-arrival-rate / login-burst: 출근 폭주 ramping / deploy-flow: 저빈도 큐 적재 / smoke), 프로파일 rps100·500·1000(1차 기준)·2000. 시드: lt_ 접두어 계정 20만(템플릿 계정 bcrypt 해시 복제)·프로그램 2천 + cleanup.sql(접두어만 제거 — 운영 데이터 무접촉 원칙 명문화). 관측: capture.sh(파드 CPU/메모리·HPA replica 추이·pg_stat_activity 커넥션·Redis 적중률 CSV) + queries.md(Hikari active/pending·Tomcat·p95/오류율 PromQL·판정 기준). 결과 형식: summary.json + CSV + RESULTS.template.md(단계별 p50/p95/p99·병목·튜닝 권장 표). 검증: k6 inspect 4스크립트 파싱 통과, bash -n.
- 2026-09-09 — Secret·이미지 수명주기 정비(9단계): 매니페스트의 자리표시자 Secret(edu-db·edu-auth-db·edu-auth-jwt, change-me 값) 전부 제거 — bootstrap 이 kind 모드는 무작위 생성, server 모드는 필수 Secret 부재 시 중단(fail-closed, placeholder 운영 반입 경로 원천 차단). 운영 반영은 Sealed Secrets 표준(deploy/k8s/secrets/README.md + edu-secrets.yaml.template, 평문 yaml 은 .gitignore 차단·봉인본만 커밋). 이미지: :latest 제거 — 매니페스트는 semver 기본(0.8.0)+imagePullPolicy IfNotPresent, bootstrap IMAGE_TAG 기본 git-<sha>(불변) 치환, Kaniko executor 도 v1.23.2 핀. CI release.yml 신설(main/v* → GHCR git-<sha>+semver push, 배포·롤백 명령 요약 출력). 롤백 = IMAGE_TAG=<이전 태그> 재배포 또는 rollout undo. 검증: change-me 잔존 0(.env.example 로컬 예시 제외), kubeconform 12리소스 Valid, 태그 치환 드라이런, bash -n, backend gradle build 회귀 통과.
- 2026-09-09 — Redis 도입(8단계): platform/redis.yaml — 캐시·rate-limit 카운터 전용(영속화 없음, maxmemory 256mb allkeys-lru, 비루트). 비밀번호는 edu-redis-auth Secret(bootstrap 이 무작위 생성, 평문 없음). backend(REDIS_HOST/PASSWORD)·auth-service(EDU_RATELIMIT_STORE=redis) env 배선, compose 에도 redis 추가(개발 경로 실사용). 두 소비자 모두 자체 폴백(캐시→DB, rate-limit→인메모리)이라 Redis 는 SPOF 아님 — HA 필요 시 Sentinel/관리형으로 env 만 교체. 검증: kubeconform Valid, bash -n.
- 2026-09-09 — DB 계층 HA·백업·풀러: postgres-ha.yaml 을 운영 프로필로 재작성(storage 50Gi·리소스 1c/2Gi~2c/4Gi·노드 분산 anti-affinity·PodMonitor) + barmanObjectStore 백업(WAL 연속 아카이브 → PITR, retention 30d, 자격 증명은 edu-db-backup-creds Secret 참조 — 평문 없음) + ScheduledBackup(매일) + PgBouncer Pooler rw/ro(transaction 모드, max_prepared_statements 로 JPA 호환). auth-db 도 동일 구성의 CNPG 로 승격(auth/auth-db-ha.yaml 신설, 20Gi·0.5c/1Gi~2c/2Gi). backend/auth-service 매니페스트 기본 DB 엔드포인트를 풀러(edu-db-pooler-rw / edu-auth-db-pooler-rw)와 오퍼레이터 생성 시크릿(edu-db-app / edu-auth-db-app)으로 전환 — bootstrap 코어(개발)는 sed 로 단일 postgres/auth-db 로 복귀. PRODUCTION.md §4 를 HA 전환·기존 데이터 마이그레이션(CNPG import / pg_dump)·PITR 복구 절차로 개정. 검증: kubeconform(+CRDs-catalog 스키마) 13리소스 Valid, 개발 모드 치환 드라이런, bash -n, SchemaMigrationTest 2종 통과.
- 2026-09-09 — 멀티 노드 운영 토폴로지 전환: PRODUCTION.md 를 단일 GPU 박스 전제에서 "워커 3+ 와 HA 컨트롤플레인(k3s embedded etcd 3대) 또는 매니지드 K8s + 앞단 L4 LB" 기준으로 개정(단일 노드는 §9 로컬·데모 전용으로 분리). backend·auth-service·frontend Deployment 에 테넌트 템플릿과 동일한 배치 정책 적용 — 무중단 롤링(maxUnavailable 0/maxSurge 1) + topologySpreadConstraints(zone·hostname, soft) + podAntiAffinity(preferred) → 멀티 노드에서 replica 자동 분산, 단일 노드(kind)에서도 스케줄 가능. PVC(postgres·auth-db)에 #EDU_STORAGE_CLASS 마커를 두고 bootstrap.sh STORAGE_CLASS 로 네트워크 스토리지 클래스 주입(local-path 노드 종속 제거, Loki·Gitea helm 에도 전달), server 모드에서 노드 3개 미만·STORAGE_CLASS 미지정 경고. ingress-nginx values 에 externalTrafficPolicy Local + 컨트롤러 노드 분산(spread/anti-affinity). PDB/HPA 불변. 검증: kubeconform 44리소스 Valid + STORAGE_CLASS 치환 드라이런 + bash -n.
- 2026-09-09 — graceful shutdown 예산: backend·auth-service Deployment 에 preStop(sleep 5 — 엔드포인트 제거 전파 대기) + terminationGracePeriodSeconds 45(앱 graceful 대기 20s + 여유) 추가. 이미지 ENTRYPOINT 가 exec java 로 바뀌어 SIGTERM 이 JVM 에 직접 전달된다(각 Dockerfile).
- 2026-09-09 — 인증 방어 하드닝: (1) K8s 매니페스트를 운영 기준 fail-safe 로 — auth-service.yaml/backend.yaml 의 `EDU_SEED` "false" 고정, auth-service 에 `EDU_DEMO_LOGIN` "false" 명시(데모 계정 시드·무비밀번호 로그인이 운영에 기본 열리지 않음). 로컬 kind 리허설은 bootstrap.sh 가 kind 모드에서만 두 값을 "true" 로 치환(시연 유지, server 모드는 그대로 꺼짐). (2) ingress.yaml 에서 /api/auth 를 전용 Ingress(edu-platform-auth)로 분리하고 엣지 rate limit(limit-rps 5 · limit-connections 10) 적용 — 앱 계층 LoginGuard(계정/IP 기준)와 이중 방어. (3) compose 는 개발 환경이므로 `EDU_DEMO_LOGIN: "true"` 명시. 검증: bash -n 문법, sed 치환 드라이런(EDU_SEED/EDU_DEMO_LOGIN true 전환 확인).
- 2026-08-26 — 인증 계층 추가: auth-service(자체 DB auth-db·BCrypt·HS256 JWT, 로컬 :8089) 분리, backend는 동일 `EDU_JWT_SECRET`으로 토큰 자체 검증(role 기반 RBAC). k8s/auth/(auth-db·auth-service·`edu-auth-jwt` Secret), docker-compose에 auth-db·auth-service·traefik 추가, apply 순서에 auth-db→auth-service(backend 앞) 반영.
- 2026-08-24 — docker-compose(postgres+backend) 작성, K8s 매니페스트(namespace·플랫폼·서비스 템플릿·RBAC) 추가.
- 2026-08-25 — docker 실배포 모드용 docker.sock/examples 마운트, 백엔드 이미지에 git/docker/kubectl.
- 2026-08-25 — 로컬 kind 리허설: Go 서비스가 Pod+Service로 기동·응답 검증. deploy/k8s/README를 K8s 배포 가이드로 리뉴얼.
- 2026-08-25 — P3-4 레지스트리 pull 경로: kind를 containerd certs.d 패턴으로 재생성(kind-with-registry.yaml) + kind-registry(127.0.0.1:5001→5000, kind net 연결) + 노드 hosts.toml(localhost:5001→kind-registry:5000). registry/ 자산(setup 스크립트/README). end-to-end 검증: Kaniko(hostAlias kind-registry) 빌드→kind-registry:5000/doc-proofreader:v1 push → localhost:5001/doc-proofreader:v1 배포 imagePullPolicy:Always → kubelet pull 221ms(digest 일치) → 파드 Running → 서비스 / ·/healthz 200. 서브에이전트 PASS. 주의: 재생성으로 관측성/보안 스택은 제거됨(각 README helm 명령으로 재설치 가능).
- 2026-08-25 — P3-2 트레이싱: Tempo(모놀리식, OTLP 4317/4318) 설치, tracing/ 자산 + Grafana Tempo 데이터소스(tracesToLogsV2→loki) + Loki derivedFields(→tempo). kind 검증: telemetrygen 20 트레이스 전송→Tempo `/api/search` 20건 조회(rootServiceName=edu-trace-test), 사이드카 로드. 서브에이전트 PASS — 로그→트레이스 provisioning url `${...}`→`$${...}` 이스케이프 결함 수정. 관측성 3축 완성.
- 2026-08-25 — P3-3 알림: Alertmanager 활성화(helm upgrade), PrometheusRule(monitoring/prometheus-rules.yaml: EduBackendDown/PodCrashLooping/HighMemory, release: kps) + README 알림 섹션. kind 검증: 규칙 4종 로드, 테스트 알림 Prometheus firing→Alertmanager active 수신, 이후 테스트 알림 비활성 재적용(실규칙 3종). 서브에이전트 PASS(job 라벨/limit 미설정 주의 반영).
- 2026-08-25 — P3-2 로그 수집: loki-stack(Loki+Promtail) 설치(helm), logging/ 자산 + Grafana Loki 데이터소스 ConfigMap(사이드카 자동 로드) + README. kind 검증: 테스트 토큰 로그→LogQL `{namespace="default"} |= "..."` status=success 5줄 매칭, 라벨 정상, 사이드카 provisioning 로드 확인. 서브에이전트 PASS(loki-stack deprecated·검증 emptyDir 반영).
- 2026-08-25 — P3-1 자동 TLS: cert-manager 설치(helm, CRDs), 발급자 체인(selfsigned→edu-root-ca(isCA)→edu-ca) 자산 + README, 테넌트 서비스 템플릿 Ingress에 cert-manager.io/cluster-issuer + spec.tls 배선. kind 검증: 발급자 Ready, 명시적 Certificate 자동발급(issuer=CN=edu-msa-root-ca), ingress-shim 주석만으로 ~3초 내 인증서/시크릿 자동 생성(SAN 일치). 서브에이전트 PASS.
- 2026-08-25 — P2-3 엣지: ingress-nginx 설치(helm, values로 ModSecurity+OWASP CRS 전역 On, TLS1.2/1.3, replica 2). edge/ 자산 + README, 테넌트 서비스 템플릿 Ingress에 rate-limit/WAF 주석. kind 검증(컨트롤러 ClusterIP --resolve): TLS 200(CN=edu-waf.internal), WAF XSS/SQLi/traversal 403, 정상 200, rate-limit 503. 서브에이전트 PASS. P2 및 전체 로드맵 완료.
- 2026-08-25 — P2-2 관측성: kube-prometheus-stack 설치(helm, alertmanager off, retention 2h). backend Service 포트명/라벨 + ServiceMonitor(platform/monitoring/) 추가, README. kind 검증: up 11타깃 스크레이프, ServiceMonitor 디스커버리(예제앱 up=1, ~30s). 백엔드는 micrometer로 /actuator/prometheus 노출.
- 2026-08-25 — P2-1 scale-to-zero: KEDA + keda-add-ons-http 설치(helm), HTTPScaledObject 템플릿(scale-to-zero-template.yaml) + platform/scale-to-zero/README 추가. kind 검증: 샘플 서비스 유휴→replicas 0, 인터셉터 경유 요청 시 0→1 콜드스타트(HTTP 200, 2.85s). 라우팅은 인터셉터(keda-add-ons-http-interceptor-proxy:8080) 경유 필수.
- 2026-08-25 — P1-3 가용성: 서비스 템플릿에 무중단 롤링(maxUnavailable:0/maxSurge:1)+PDB(maxUnavailable:1)+topologySpread(AZ)/anti-affinity(soft). kind 검증: 롤링 중 무중단(220요청 실패 1), PDB ALLOWED=1.
- 2026-08-25 — P1-1 Kaniko 안전빌드: 백엔드 real 모드가 docker.sock 대신 Kaniko Job으로 빌드. kind 검증: 실제 GitHub 레포 빌드→인클러스터 레지스트리 push. repoUrl/branch 주입 검증 추가.
- 2026-08-25 — P1-2 NetworkPolicy 강제: kind를 disableDefaultCNI로 재생성 + Calico v3.28 설치. hardening NetworkPolicy 적용 후 검증 — 공개 tier 외부 egress 차단(DNS만), 내부 tier 인터넷 허용, 정책없는 ns 개방. (kindnet에선 미강제였음)
- 2026-08-25 — P0-3 DB HA: CloudNativePG 오퍼레이터 + postgres-ha.yaml(Cluster instances 3), backend.yaml을 edu-db-rw + edu-db-app 시크릿으로 전환. kind 검증: 3/3 healthy, primary 삭제→복제본 자동 승격(edu-db-1→2)→재수렴. P0 전부 완료.
- 2026-08-25 — 대규모 로드맵(ROADMAP.md) 착수. P0-1 오토스케일: platform/autoscale.yaml(HPA+PDB), 서비스 템플릿 HPA. kind에서 metrics-server + HPA `cpu:1%/70%` 판독 검증.
- 2026-08-25 — 멀티테넌트 보안 하드닝(hardening/) 추가: 신뢰 등급별 네임스페이스(baseline/restricted), ResourceQuota/LimitRange, NetworkPolicy(deny-by-default), gVisor RuntimeClass, Kaniko 빌드 Job 템플릿. 서비스 템플릿에 securityContext 강화. kind에서 restricted 루트 파드 거부 검증.
